<div class='row-fluid margin'>
	<div class='form-search'>
		<% if @questions.present? %>
			<h2><%= @questions.count %> questions detected </h2>

			<% @questions.each do |question| %>
				<p><%= link_to question.title, question_path(question) %></p>
			<% end -%>
		<% end -%>
	</div>
</div>